{
  "name": "prgenie",
  "private": true,
  "version": "0.1.0",
  "description": "Local pull requests for agent work. GitHub when you say so.",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "tsx --test packages/core/src/prs.test.ts packages/core/src/github.test.ts packages/core/src/store.test.ts packages/core/src/watch.test.ts packages/cli/src/mcp-stdio.test.ts",
    "typecheck": "tsc -p packages/core --noEmit && tsc -p packages/cli --noEmit && tsc -p packages/extension --noEmit",
    "cli": "node packages/cli/dist/prgenie.cjs",
    "link-plugin": "powershell -ExecutionPolicy Bypass -File scripts/link-plugin.ps1",
    "link-extension": "powershell -ExecutionPolicy Bypass -File scripts/link-extension.ps1"
  },
  "devDependencies": {
    "@types/node": "^22.17.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.25.9",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
